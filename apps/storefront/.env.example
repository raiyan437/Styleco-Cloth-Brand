# Optional: copy to apps/storefront/.env.local. Defaults work without this file.
CATALOG_PROVIDER=mock

# Reserved for the future adapter; not read or required in mock mode.
APPWRITE_ENDPOINT=http://localhost/v1
APPWRITE_PROJECT_ID=
APPWRITE_DATABASE_ID=
